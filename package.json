{
  "type": "module",
  "apiConfig": {
    "baseUrl": "http://localhost:5001",
    "timeout": "5000"
  },
  "dependencies": {
    "browser-image-compression": "^2.0.2",
    "firebase": "^11.5.0",
    "qrcode": "^1.5.4",
    "typescript": "^5.8.2"
  },
  "devDependencies": {
    "@types/qrcode": "^1.5.5",
    "vite": "^6.2.4",
    "vite-plugin-static-copy": "^3.0.0"
  },
  "scripts": {
    "dev": "node scripts/generate-dev-route && vite",
    "build": "node scripts/generate-prod-route && vite build",
    "preview": "vite preview"
  }
}
